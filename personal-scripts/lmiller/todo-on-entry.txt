sudo passwd sysadmin
sudo passwd root

sudo yum update

sudo yum install -y git
git clone https://github.com/CedarvilleCyber/CCDC

cd CCDC/linux-scripts
sudo ./pansophy.sh

cd ../personal-scripts/lmiller/
sudo ./starlight

sudo ./check-integrity (for prestashop)

# Helpful hints
tmux ls to list tmux sessions
tmux a -t <session_name> to attach to a session
